# Puma-CPU-scheduler
Implemented a priority-based CPU scheduler in C using linked-list queues. The scheduler manages process lifecycles, selects runnable processes, and prevents starvation through aging and priority promotion, with explicit memory management and bitwise state tracking.
